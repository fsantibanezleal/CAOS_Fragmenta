# The runtime lane. Deliberately thin: everything the browser needs is baked into the artifacts or
# implemented in TypeScript, so nothing here is required to SERVE the site.
#
# Present so that a reader can load and inspect a baked artifact without the heavy lane.
numpy>=1.24,<3
