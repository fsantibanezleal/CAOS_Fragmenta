# The heavy lane: the bake. Pinned, because a canonical artifact must be reproducible.
#
# The science lives in blastfrag, a SEPARATE published repo this product consumes. A product declares
# no package of its own; see the engine repo for the models themselves.
#
# Installed from a git tag until the PyPI trusted publisher is registered, at which point this line
# becomes `blastfrag==0.2.0`. The tag is what makes the bake reproducible in the meantime.
blastfrag @ git+https://github.com/fsantibanezleal/CAOS_BlastFrag@v0.02.000

numpy>=1.24,<3
scikit-learn>=1.3,<2
xgboost>=2.0,<4
