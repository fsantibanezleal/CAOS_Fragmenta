# The heavy lane: the bake.
#
# EXACTLY pinned, not ranged. A canonical scientific artifact built from a version RANGE is not
# reproducible by anyone else, and the failure is silent: the numbers come out slightly different and
# every gate still passes because nothing compares across machines.
#
# This repo's determinism check does compare across machines, and it is what caught the range. A bake
# is a pure function of the case registry, the seed AND this file.
#
# Bumping any pin below means re-running the full bake and committing the artifacts in the same
# change, because every number in every artifact came from these versions.
#
# The science lives in blastfrag, a SEPARATE published package this product consumes (PyPI, trusted
# publishing since 2026-09-26). A product declares no package of its own.
blastfrag==0.2.2

numpy==2.5.3
scikit-learn==1.9.0
xgboost==3.4.1
